# 🇦🇴 Validador de NIF Angolano

API REST simples e documentada para validar **Números de Identificação Fiscal (NIF)** de Angola, tanto para **Pessoa Singular** quanto **Pessoa Coletiva**.

- ✅ Validação por formato e província (para NIF singular)
- ✅ Swagger UI integrada
- ✅ Leve, rápida e fácil de implantar
- ✅ Totalmente em Go

---

## 📦 Estrutura do NIF

### Pessoa Coletiva

- **10 dígitos numéricos** Ex: `5000000000`

### Pessoa Singular

- **14 caracteres**: `9 dígitos` + `2 letras (província)` + `3 dígitos`  
  Ex: `123456789LA001`

#### Siglas de províncias válidas:

`BG`, `BN`, `IB`, `BO`, `CA`, `CU`, `CB`, `CN`, `CS`, `CL`, `HA`, `HL`, `LA`, `LS`, `LN`, `MA`, `MO`, `ML`, `NA`, `UI`, `ZA`

---

## 🚀 Pré-requisitos

- **Go 1.21 ou superior** Verifique com:
  ```bash
  go version
  ```

# 🇦🇴 Validador de NIF Angolano

API REST simples e documentada para validar **Números de Identificação Fiscal (NIF)** de Angola, tanto para **Pessoa Singular** quanto **Pessoa Coletiva**.

- ✅ Validação por formato e província (para NIF singular)
- ✅ Swagger UI integrada
- ✅ Leve, rápida e fácil de implantar
- ✅ Totalmente em Go

---

## 📦 Estrutura do NIF

### Pessoa Coletiva

- **10 dígitos numéricos** Ex: `5000000000`

### Pessoa Singular

- **14 caracteres**: `9 dígitos` + `2 letras (província)` + `3 dígitos`  
  Ex: `123456789LA001`

#### Siglas de províncias válidas:

`BG`, `BN`, `IB`, `BO`, `CA`, `CU`, `CB`, `CN`, `CS`, `CL`, `HA`, `HL`, `LA`, `LS`, `LN`, `MA`, `MO`, `ML`, `NA`, `UI`, `ZA`

---

## 🚀 Pré-requisitos

- **Go 1.21 ou superior** Verifique com:
  ```bash
  go version
  ```
